name: Test

on:
  workflow_dispatch:

jobs:
  update-token-list:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Set up Git configuration
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git config core.autocrlf input

      - name: Remove unwanted files and directories
        run: |
          rm -rf .github/workflows

      - name: Append new token to FXList.json
        run: |
          # Create token_entry.json
          jq -n \
            --arg name "Test" \
            --argjson chainId 1 \
            --arg symbol "TT" \
            --argjson decimals 18 \
            --arg address "0xabc1234" \
            --arg logoURI "https://raw.githubusercontent.com/isabelle-pundix/FXSwap-TokenList//Tokens/0xabc1234/logo.png" \
            '{name: $name, chainId: $chainId, symbol: $symbol, decimals: $decimals, address: $address, logoURI: $logoURI}' > token_entry.json

          # Append the new entry to FXList.json
          jq '. + [$token_entry]' FXList.json --argjson token_entry "$token_entry" > tmp.FXList.json && mv tmp.FXList.json FXList.json

      - name: Commit changes
        run: |
          git checkout -b test
          git add FXList.json 
          git commit -m "test"
          git push origin test
